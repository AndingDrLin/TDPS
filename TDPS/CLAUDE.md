# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This inner project file intentionally defers to the repository root guidance.

Use `/Users/linyujia/Project/TDPS/CLAUDE.md` as the source of truth for Claude Code instructions in this repository. If this file conflicts with the root document, follow the root document.

Most commands in the root `CLAUDE.md` assume the outer repository root as the current working directory and therefore start with `TDPS/`. If Claude Code is launched from this inner `TDPS/` directory, remove that leading `TDPS/` path component.
